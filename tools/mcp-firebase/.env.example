# --- RAGESTATE Firebase MCP Server Environment ---

# Required: Path to your Google Cloud service account JSON key.
# This is used to authenticate the Firebase Admin SDK.
# Example: GOOGLE_APPLICATION_CREDENTIALS=../../secrets/firebase-mcp-sa.json
GOOGLE_APPLICATION_CREDENTIALS=

# Required: Your Firebase Project ID.
GCP_PROJECT=ragestate-app

# Optional: A secret API key to protect the MCP server endpoints.
# If set, requests must include an 'x-mcp-api-key' header with this value.
MCP_API_KEY=

# Optional: Port for the server to listen on. Defaults to 8765.
# PORT=8765

# --- For Local Development with Emulators ---

# Optional: If you are running the Firestore emulator, set this variable.
# The server will connect to the emulator instead of production Firestore.
# Example: FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
FIRESTORE_EMULATOR_HOST=
